<Inspect>
  <AllowedIPs>
    <!-- 
      Add the IP addresses that should be allowed to call the inspector as shown in the example below.
      There is no need to add 127.0.0.1, this IP is always allowed.
    -->
    <!--<AllowedIP>127.0.0.1</AllowedIP>-->
  </AllowedIPs>
</Inspect>